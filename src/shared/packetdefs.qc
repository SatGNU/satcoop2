
/* These are toplevel packet identifiers. They are sent as a byte, so
 0-255 range. Ideally for complex packets that require it, a second byte
 can be sent with a packet subtype. */
const float PACKET_SC_SOMETHING = 0;